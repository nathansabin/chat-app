export const setName = (state) => ({
    type: 'SET_NAME',
    payload: state
});

export const setConnection = (state) => ({
    type: 'SET_CONNECT',
    payload: state
});

export const pushLog = (state) => ({
    type: 'SET_NAME',
    payload: state
});
